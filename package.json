{
  "name": "reorderable",
  "version": "0.1.0",
  "description": "Drag and drop list reordering for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.21.5",
    "obsidian": "latest",
    "tslib": "2.6.2",
    "typescript": "^5.4.5"
  }
}
